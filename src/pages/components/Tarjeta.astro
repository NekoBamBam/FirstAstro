---
const { titulo, descripcion } = Astro.props;
---

<div class="p-4 border border-gray-300 rounded-xl bg-white shadow-sm mb-4">
  <h2 class="text-xl font-semibold mb-2">{titulo}</h2>
  <p class="text-gray-600">{descripcion}</p>
</div>
